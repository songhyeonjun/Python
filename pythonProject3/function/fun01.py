# 함수(function, 펑션, 기능)
# 기능 : 동적인 처리
# kakao api 코드
# 1) post방식으로 kakao site에 접속
# 2) 이미지 url를 분석해서 자동으로 태그붙인 결과를 json으로 리턴
# 3) json으로 받은 결과를 프린트
# connect이름으로 1~3절차를 "정의"해두고
# 이 기능이 필요할 때마다 불러서(call) 여러번 사용 가능하게 하자.
# 함수를 쓰려면,
# (1)함수에 어떤 기능을 정의할지 결정
# (2)필요할 때마다 정의한 기능을 실행하도록 호출하여 사용
# 함수의 기능을 사용할 수 있는 방법 3가지
# (1) python install시 많은 함수가 이미 download(내장된 함수)
# (2) 인터넷 저장소에 추가로
#     많은 함수들 묶음(라이브러리, 패키지)이 있으니 필요할 때 download
# (3) 내가 만든다.
